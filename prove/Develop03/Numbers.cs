using System.Diagnostics;
using System.Runtime.CompilerServices;

public class Numbers
{
    private List<double> _numbersList = new List<double>(); //the variable must be private

    public void AddNumber(double number){
        //Process of adding numbers to the list
    }
    public double GetNumber(int index)
    {
        double number=0;
        // getting code from the list
        return number;
    }
    public void OrderList(bool asc = true) //it order the numbers list by a criterion, Ascendent(asc) is true by default
    {
        //Process of ordering list
    }
}